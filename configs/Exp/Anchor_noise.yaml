# @package _global_
defaults:
  - override /dataset: cifar100
  - _self_


device:
  id: 1

optimizer:
  _target_: torch.optim.AdamW
  lr: 1e-4
  weight_decay: 2e-6

# ===========================
process_title: Anchor_noise|without
# ===========================

clip:
  add_noise_for_anchors: False