# @package _global_
defaults:
  - override /dataset: cifar100
  - _self_


device:
  # cuda_visible_devices:  "0,1,2,3,4,5,6,7"
  cuda_visible_devices:  "0,1,2,3"
  id: 2

optimizer:
  _target_: torch.optim.AdamW
  lr: 1e-3
  weight_decay: 1e-2

# ===========================
process_title: 062002_cifar100_E10_LR1e-3
# ===========================

trainer:
  max_epochs: 10