# @package _global_
defaults:
  - override /dataset: cifar100
  - _self_


device:
  # cuda_visible_devices:  "0,1,2,3,4,5,6,7"
  cuda_visible_devices:  "0,1,2,3"
  id: 1

optimizer:
  _target_: torch.optim.SGD
  lr: 0.001
  momentum: 0.9
  weight_decay: 1e-4

# ===========================
process_title: 062020_cifar100_sgd_baseline_noT2T
# ===========================

clip:
  text_to_text_enable: False