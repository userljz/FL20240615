# @package _global_
defaults:
  - override /dataset: cifar100
  - _self_


device:
  # cuda_visible_devices:  "0,1,2,3,4,5,6,7"
  cuda_visible_devices:  "0,1,2,3"
  id: 0

optimizer:
  _target_: torch.optim.AdamW
  lr: 1e-4
  weight_decay: 2e-6

# ===========================
# 这里开始，以及以后，使用的都是 main branch 的 set_param，get_param，aggregation 函数
process_title: 062009_cifar100_noNoise
# ===========================

clip:
  add_noise_for_anchors: False